"""Lightweight rules engine for static IaC checks.

This package is intentionally self-contained and import-safe. It loads
YAML-defined rules and evaluates them against IaC files alongside the
existing ML models.
"""
