# Auto-discovered rule — generated 2026-03-11T07:55:37.065436
# Based on 7 occurrences across 7 scans
# Original: Detected a 3-hop attack path from aws_security_group.web_sg to aws_db_instance.database.

**Attack Chain:** web_sg → web
rules:
  - id: DISC_2D5F8C1711A2
    description: "[Auto-Discovered] Detected a 3-hop attack path from aws_security_group.web_sg to aws_db_instance.database.

**Attack C"
    severity: CRITICAL
    file_types: [".tf", ".yaml", ".yml", ".json"]
    match:
      regex: "aws_security_group.*aws_db_instance"
    references:
      - "https://cloudguard.ai/discovered-patterns/2d5f8c1711a2"
