# Auto-discovered rule — generated 2026-03-11T07:55:38.135100
# Based on 4 occurrences across 4 scans
# Original: Detected a 3-hop attack path from aws_subnet.web_subnet to aws_s3_bucket.flowbucket.

**Attack Chain:** web_subnet → web
rules:
  - id: DISC_C9C05E3D5605
    description: "[Auto-Discovered] Detected a 3-hop attack path from aws_subnet.web_subnet to aws_s3_bucket.flowbucket.

**Attack Chain"
    severity: MEDIUM
    file_types: [".tf", ".yaml", ".yml", ".json"]
    match:
      regex: "flowbucket.*web_subnet"
    references:
      - "https://cloudguard.ai/discovered-patterns/c9c05e3d5605"
