# Auto-discovered rule — generated 2026-03-11T07:55:37.641870
# Based on 6 occurrences across 3 scans
# Original: S3 bucket 'bad' does not have access logging enabled. CIS Benchmark 2.1.2 requires logging for audit trails.

**Impact:*
rules:
  - id: DISC_8194E398FA0F
    description: "[Auto-Discovered] S3 bucket 'bad' does not have access logging enabled. CIS Benchmark 2.1.2 requires logging for audit"
    severity: MEDIUM
    file_types: [".tf", ".yaml", ".yml", ".json"]
    match:
      regex: "information.*disclosure"
    references:
      - "https://cloudguard.ai/discovered-patterns/8194e398fa0f"
