[tool.pytest.ini_options]
testpaths = ["tests", "api/tests", "ml/tests"]
pythonpath = [".", "api", "ml", "rules"]
asyncio_mode = "strict"
addopts = "-q --tb=short --import-mode=importlib"
filterwarnings = [
    "ignore::FutureWarning:torch.*",
    "ignore::DeprecationWarning:torch_geometric.*",
]
